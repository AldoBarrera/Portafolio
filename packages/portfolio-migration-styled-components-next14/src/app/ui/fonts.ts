import { Manrope, Nunito } from 'next/font/google';

export const manrope = Manrope({
  weight: ['500', '700'],
  subsets: ['latin'],
});

export const nunito = Nunito({
  weight: ['400'],
  subsets: ['latin'],
});
